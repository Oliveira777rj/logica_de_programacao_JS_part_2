// Escreva uma função que recebe dois valores, sendo, o primeiro é a base o segundo a potência

function potencia(a,b){

    //return Math.pow(a,b)

    // Nova função no JS para realizar calculo de potência
    return a ** b
}

console.log(potencia(2,4))

console.log(potencia(2,8))

// Nova função no JS para realizar calculo de potência



// Escreva uma função que some dois valores e imprima o resultado no console

function soma(a,b){

    let resultado = a + b
    console.log(`A soma de ${a} e ${b} = ${resultado}`)
}

soma(10,1)

soma(20,20)